name: calendar_view
description: A Flutter package allows you to easily implement all calendar UI
  and calendar event functionality.
version: 1.0.5
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  collection: ^1.17.1
  flutter:
    sdk: flutter
  intl: ^0.18.1
  provider: ^6.0.5
  table_calendar: ^3.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  very_good_analysis: ^5.1.0

flutter: null
